# examples-for-eos-vm

Examples for using eos-vm and some smart contracts.

Just use cmake to build the project.

The contracts are in the contracts/src folder

The output will be a driver (eos-vm driver) and a hello.wasm and print_num.wasm.
